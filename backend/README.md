# backend

业务后端模块，技术栈按定稿使用 Python + FastAPI。

当前阶段只落工程骨架，后续按文档逐步补齐：

- `app/api`：手机端与管理端 API 路由
- `app/core`：配置、启动、数据库连接、基础中间件
- `app/services`：业务服务层
- `app/models`：数据库模型
- `app/schemas`：请求/响应结构
- `app/repositories`：数据访问层

本阶段不提前实现复杂业务逻辑。
