# 详细流程图

本文用于从“流程结构”的角度说明项目运行逻辑，和时序图互补：

- 时序图强调“谁先调用谁”
- 流程图强调“整体步骤如何推进、分支如何选择”

本文同样遵循当前代码的真实实现，不引入未来规划中的假设流程。

---

## 0. 全局总流程图

这张图用于先把整个系统的主路径分清楚：
手机侧有两条主线，分别是“手机独立拍摄”与“设备联动”；
AI 也分成“后端 AI”与“设备端 AI”两套逻辑。

```mermaid
flowchart TD
  Start["用户进入系统"] --> Entry{"选择功能入口"}

  Entry --> MobileOnly["手机独立拍摄"]
  Entry --> DeviceLink["设备联动"]

  MobileOnly --> M1["拉取模板 / 套餐 / 历史"]
  M1 --> M2["打开手机相机"]
  M2 --> M3["拍照"]
  M3 --> M4["创建后端 session"]
  M4 --> M5["上传图片到 backend"]
  M5 --> M6["创建 capture 历史记录"]
  M6 --> M7{"是否发起后端 AI"}
  M7 -->|是| M8["创建 ai_task 并调用 AI Provider"]
  M8 --> M9["返回 AI 结果并写历史"]
  M7 -->|否| M10["仅保留拍摄历史"]

  DeviceLink --> D1["打开设备联动页"]
  D1 --> D2["打开树莓派设备会话"]
  D2 --> D3["启动手机推流"]
  D3 --> D4["树莓派实时检测 / 构图 / overlay"]
  D4 --> D5["手机接收预览回传"]
  D5 --> D6{"选择设备侧功能"}

  D6 --> D7["手动控制 / 模式切换 / 模板下发"]
  D6 --> D8["自动找角度"]
  D6 --> D9["背景锁机位"]
  D6 --> D10["手动抓拍 / 手势抓拍"]

  D8 --> D11["树莓派扫描候选角度并调用设备端 AI"]
  D9 --> D12["树莓派扫描候选机位并调用设备端 AI"]
  D10 --> D13["保存到 device_runtime/captures"]

  M9 --> End["结果展示 / 历史查看"]
  M10 --> End
  D7 --> End
  D11 --> End
  D12 --> End
  D13 --> End
```

### 图意说明

这张总流程图对应三条关键结论：

1. 手机独立拍摄与设备联动是两条不同主线。
2. 后端 AI 与设备端 AI 是两种不同能力，不共享同一条运行流程。
3. 设备抓拍默认进入本地 `captures`，不是自动写后端历史。

---

## 1. 手机独立拍摄总流程

这张图只关注手机拍照、上传、入历史、可选 AI 分析。

```mermaid
flowchart TD
  A0["进入拍摄页"] --> A1["请求模板列表"]
  A1 --> A2{"模板请求成功?"}
  A2 -->|是| A3["展示最新模板"]
  A2 -->|否且有缓存| A4["展示本地缓存模板"]
  A2 -->|否且无缓存| A5["提示模板加载失败"]

  A3 --> A6["初始化手机相机"]
  A4 --> A6
  A5 --> A6

  A6 --> A7["用户点击拍照"]
  A7 --> A8["takePicture 获取本地照片"]
  A8 --> A9["创建 capture_session"]
  A9 --> A10["上传图片文件"]
  A10 --> A11["创建 capture 记录"]
  A11 --> A12["写入本地历史缓存"]
  A12 --> A13{"是否继续 AI 分析?"}

  A13 -->|是| A14["调用后端 AI 接口"]
  A14 --> A15["返回 AI 结果"]
  A15 --> A16["刷新历史记录并展示结果"]

  A13 -->|否| A17["只保留拍摄历史"]
```

---

## 2. 后端 AI 分析流程

这张图对应手机独立拍摄后的 AI 任务创建与执行。

```mermaid
flowchart TD
  B0["用户发起 AI 分析"] --> B1["backend 校验 session / capture"]
  B1 --> B2{"校验通过?"}

  B2 -->|否| B3["返回错误"]

  B2 -->|是| B4["解析当前用户可用 AI Provider"]
  B4 --> B5{"Provider 已配置且可用?"}

  B5 -->|否| B6["创建 failed ai_task"]
  B6 --> B7["返回失败信息"]

  B5 -->|是| B8["创建 pending ai_task"]
  B8 --> B9["调用 AI Provider"]
  B9 --> B10{"Provider 调用成功?"}

  B10 -->|否| B11["更新 ai_task 为 failed"]
  B11 --> B12["返回失败结果"]

  B10 -->|是| B13["更新 ai_task 为 succeeded"]
  B13 --> B14["返回评分 / 总结 / 建议"]
```

---

## 3. 设备联动总流程

这张图只关注设备联动主循环。

```mermaid
flowchart TD
  C0["进入设备联动页"] --> C1["加载后端模板"]
  C1 --> C2["加载树莓派本地模板"]
  C2 --> C3["恢复最近连接配置"]
  C3 --> C4["启动状态轮询"]
  C4 --> C5["用户打开设备会话"]
  C5 --> C6["tree莓派创建当前 session"]
  C6 --> C7["用户启动手机推流"]
  C7 --> C8["手机采集 NV21 帧"]
  C8 --> C9["WS mobile-ws 推送到树莓派"]
  C9 --> C10["树莓派帧循环检测"]
  C10 --> C11["计算 tracking / compose / overlay / AI 状态"]
  C11 --> C12{"当前模式需要控制云台?"}
  C12 -->|是| C13["输出 pan / tilt 控制"]
  C12 -->|否| C14["保持当前状态"]
  C13 --> C15["WS preview-ws 回传预览"]
  C14 --> C15
  C15 --> C16["手机 HUD 展示状态和预览"]
  C16 --> C17{"用户是否触发设备侧功能?"}
  C17 -->|是| C18["进入设备侧功能分支"]
  C17 -->|否| C8
```

---

## 4. 设备联动功能分支图

这张图把设备联动里几个主要能力拆开。

```mermaid
flowchart TD
  D0["设备联动进行中"] --> D1{"用户选择功能"}

  D1 --> D2["手动移动云台"]
  D1 --> D3["切换模式"]
  D1 --> D4["选择 / 下发模板"]
  D1 --> D5["自动找角度"]
  D1 --> D6["背景锁机位"]
  D1 --> D7["手动抓拍"]
  D1 --> D8["等待手势抓拍"]

  D2 --> D9["发送 manual-move 指令"]
  D3 --> D10["设置 MANUAL / AUTO_TRACK / SMART_COMPOSE"]
  D4 --> D11["选择后端模板或设备模板"]
  D5 --> D12["进入角度搜索流程"]
  D6 --> D13["进入背景锁定流程"]
  D7 --> D14["保存本地抓拍"]
  D8 --> D15["实时检测手势并可能进入倒计时"]
```

---

## 5. 自动找角度流程

```mermaid
flowchart TD
  E0["用户启动自动找角度"] --> E1["设备端创建 angle search 任务"]
  E1 --> E2["根据扫描参数生成候选角度"]
  E2 --> E3["云台逐个转到候选位置"]
  E3 --> E4["保存每个候选位置的当前帧"]
  E4 --> E5{"候选角度是否扫描完成?"}
  E5 -->|否| E3
  E5 -->|是| E6["设备端 AI 批量分析候选图"]
  E6 --> E7["选出最佳角度建议"]
  E7 --> E8["写入 ai_status / last_angle_search_result"]
  E8 --> E9{"用户是否应用建议?"}
  E9 -->|是| E10["执行 apply-angle 并驱动云台转动"]
  E9 -->|否| E11["仅展示建议结果"]
```

---

## 6. 背景锁机位流程

```mermaid
flowchart TD
  F0["用户启动背景锁机位"] --> F1["设备端创建 background lock 任务"]
  F1 --> F2["根据扫描参数生成候选机位"]
  F2 --> F3["云台逐个移动到候选机位"]
  F3 --> F4["保存候选机位画面"]
  F4 --> F5{"候选机位是否扫描完成?"}
  F5 -->|否| F3
  F5 -->|是| F6["设备端 AI 分析人物与背景关系"]
  F6 --> F7["选出推荐机位和 target_box"]
  F7 --> F8["写入 ai lock 状态"]
  F8 --> F9{"用户是否应用锁定建议?"}
  F9 -->|是| F10["执行 apply-lock 并驱动云台转动"]
  F9 -->|否| F11["仅展示锁定建议"]
```

---

## 7. 手势抓拍流程

```mermaid
flowchart TD
  G0["实时推流中"] --> G1["树莓派持续做人体/手部检测"]
  G1 --> G2["GestureCaptureState 更新状态"]
  G2 --> G3{"是否识别到有效手势?"}

  G3 -->|否| G1
  G3 -->|是| G4["启动 3 秒倒计时"]
  G4 --> G5["overlay 和 status 暴露剩余秒数"]
  G5 --> G6{"倒计时期间手势仍然有效?"}

  G6 -->|否| G7["取消倒计时并回到检测"]
  G7 --> G1

  G6 -->|是| G8{"倒计时结束?"}
  G8 -->|否| G5
  G8 -->|是| G9["调用 trigger_capture"]
  G9 --> G10["保存当前处理帧到本地 captures"]
  G10 --> G11["更新 latest_capture_result / analysis"]
```

---

## 8. 设备手动抓拍流程

```mermaid
flowchart TD
  H0["用户点击抓拍"] --> H1["POST /api/device/capture/trigger"]
  H1 --> H2["从当前处理帧生成图片"]
  H2 --> H3["保存到 device_runtime/captures"]
  H3 --> H4{"是否开启设备端自动分析?"}
  H4 -->|否| H5["直接返回 capture_path"]
  H4 -->|是| H6["执行设备端本地分析"]
  H6 --> H7["返回 capture_path + analysis"]
```

---

## 9. 模板处理流程

这张图把“手机模板”和“树莓派本地模板”区分开。

```mermaid
flowchart TD
  I0["进入模板相关功能"] --> I1{"模板来源?"}

  I1 -->|后端用户模板| I2["请求 /mobile/templates"]
  I2 --> I3["展示用户模板或推荐模板"]
  I3 --> I4{"是否上传新模板?"}
  I4 -->|是| I5["上传模板照片到 backend"]
  I5 --> I6["后端生成 template_data"]
  I6 --> I7["写入 templates 表"]
  I4 -->|否| I8["保持当前模板列表"]

  I1 -->|树莓派本地模板| I9["请求 /api/device/templates"]
  I9 --> I10["展示设备本地模板"]
  I10 --> I11{"是否上传到树莓派?"}
  I11 -->|是| I12["上传图片到树莓派"]
  I12 --> I13["树莓派生成本地模板并可自动选中"]
  I11 -->|否| I14["保持当前设备模板列表"]
```

---

## 10. 历史记录与缓存回退流程

```mermaid
flowchart TD
  J0["用户打开历史页"] --> J1["请求后端 sessions + captures"]
  J1 --> J2{"网络与接口是否正常?"}

  J2 -->|是| J3["返回最新历史记录"]
  J3 --> J4["写入本地缓存"]
  J4 --> J5["展示最新历史"]

  J2 -->|否| J6["读取本地缓存"]
  J6 --> J7{"本地缓存是否存在?"}
  J7 -->|是| J8["展示缓存历史"]
  J7 -->|否| J9["显示错误提示"]
```

---

## 11. 当前项目的流程边界总结

```mermaid
flowchart LR
  A["手机独立拍摄"] --> A1["后端 session"]
  A1 --> A2["上传图片"]
  A2 --> A3["写 capture 历史"]
  A3 --> A4["可选后端 AI"]

  B["设备联动"] --> B1["打开设备会话"]
  B1 --> B2["手机推流"]
  B2 --> B3["树莓派实时检测"]
  B3 --> B4["云台控制 / overlay / 预览回传"]
  B4 --> B5["设备端 AI / 本地抓拍"]

  C["本地缓存回退"] --> C1["模板缓存"]
  C --> C2["历史缓存"]

  D["当前未自动打通"] --> D1["设备抓拍自动写后端历史"]
  D --> D2["手机独立拍照离线补传"]
```

### 总结

当前流程图可以归纳成四句话：

1. 手机独立拍摄是“后端主导”的业务流程。
2. 设备联动是“树莓派运行时主导”的现场流程。
3. 设备端 AI 与后端 AI 是两套独立流程。
4. 当前已经有缓存回退与本地抓拍保底，但没有完整离线补传闭环。
