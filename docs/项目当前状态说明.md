# 项目当前状态说明

## 文档用途

这份文档用于快速说明项目在当前时间点的真实状态，方便：

- 新开对话窗口时给 Codex 快速接手
- 团队成员了解当前完成度
- 联调、验收、演示前快速确认项目边界

它和其他核心文档的关系如下：

- [开发定稿.md](</E:/BianChengWenJian/Camera Assistant/docs/开发定稿.md:1>)
  - 作用：产品目标、模块边界、功能设计的最终基线
  - 用途：回答“项目最终要做成什么样”
- [Codex执行开发计划.md](</E:/BianChengWenJian/Camera Assistant/docs/Codex执行开发计划.md:1>)
  - 作用：批次与阶段拆分、实施顺序、里程碑基线
  - 用途：回答“项目按什么顺序推进、现在做到第几批”
- [开发进展记录.md](</E:/BianChengWenJian/Camera Assistant/docs/开发进展记录.md:1>)
  - 作用：逐次改动的流水记录
  - 用途：回答“最近改了什么、当前遗留什么”
- 本文档
  - 作用：给出当前项目的整体状态快照
  - 用途：回答“现在整个项目是什么状态、还能做什么、要怎么启动”

## 是否需要继续参考《开发定稿》和《Codex执行开发计划》

需要，而且仍然是最高优先级的两份参考文档。

建议遵循下面的参考顺序：

1. 先看 [开发定稿.md](</E:/BianChengWenJian/Camera Assistant/docs/开发定稿.md:1>)
   - 用来确认产品目标、页面职责、模块关系
2. 再看 [Codex执行开发计划.md](</E:/BianChengWenJian/Camera Assistant/docs/Codex执行开发计划.md:1>)
   - 用来确认批次、阶段、开发顺序、验收边界
3. 再看 [项目当前状态说明.md](</E:/BianChengWenJian/Camera Assistant/docs/项目当前状态说明.md:1>)
   - 用来确认当前真实完成度和运行状态
4. 最后看 [开发进展记录.md](</E:/BianChengWenJian/Camera Assistant/docs/开发进展记录.md:1>)
   - 用来确认最近一次修改细节

简化理解：

- `开发定稿` 决定“应该做成什么”
- `执行开发计划` 决定“应该怎么推进”
- `项目当前状态说明` 决定“现在实际到了哪里”
- `开发进展记录` 决定“最近具体改了什么”

## 当前总进度

按当前代码与文档对齐后的状态：

- 批次 1：已完成
- 批次 2：已完成
- 批次 3：已完成
- 批次 4：已完成
- 批次 5：已完成
- 批次 6：已完成核心闭环，并已接入真实 AI 调用框架
- 批次 7：已完成
- 批次 8：已进入联调与收尾阶段

当前总体判断：

- 项目已从“搭骨架”阶段进入“联调、验收、收尾”阶段
- 主链已经具备可运行能力
- 后续重点不再是大面积新建模块，而是：
  - 手工验收
  - 剩余问题收口
  - 部署与演示准备

## 当前模块状态

### 1. backend

当前状态：主链已可用

已完成：

- FastAPI 骨架
- PostgreSQL 接入
- 核心数据模型：
  - 用户
  - 套餐
  - 订阅
  - 模板
  - 拍摄会话
  - 抓拍记录
  - AI 任务
  - AI Provider 配置
- mobile API
- admin API
- 登录鉴权
- 图片上传到 backend 本地静态目录
- `analyze-photo / analyze-background / batch-pick` 的真实 provider 调用框架
- AI Provider 多厂商、多模型、多密钥配置管理
- 套餐级 AI 配置选择逻辑

当前特点：

- 真实 AI 调用框架已经接入
- 图片上传链路已改为真实上传，不再只是本地 `file://` 元数据
- 当前仍然以“本地静态存储”作为上传图片存储方案

### 2. device_runtime

当前状态：主链已可用，可在本机模拟树莓派运行

已完成：

- `health`
- `status`
- `session/open`
- `session/close`
- `manual-move`
- `mode`
- `follow-mode`
- 模板 `import / list / select / delete`
- `apply-angle`
- `apply-lock`
- `capture/trigger`
- `preview.jpg` 设备预览接口

当前特点：

- 设备端本地 API 已能独立验收
- 可以先在本机模拟运行，不强依赖真实树莓派部署
- 首版预览采用“最新帧 JPEG 预览”，不是低延迟实时视频

### 3. mobile_client

当前状态：主链可用，已完成一轮产品化整改

已完成：

- Flutter 工程骨架
- 登录
- 登录态恢复
- 首页
- 拍照页
- 模板列表与选择
- 图片上传
- 单图分析
- 背景分析
- 历史会话 / 历史抓拍
- 设备联动页
- 本地缓存
- 设备结果回流
- batch-pick 最小入口

本轮已经完成的移动端整改：

- 设备联动页布局重构
- 设备联动页首版画面预览
- backend URL 图片预览支持
- 首页与登录页文案收口
- 网络错误提示统一优化
- 一轮移动端统一回归

当前特点：

- 不是只停留在“接口能通”，而是已经做过一轮产品层整改
- 设备联动页已不再只是控制表单，也具备首版预览
- 历史页和拍照页已支持 backend `uploads` URL 图片显示

### 4. admin_web

当前状态：管理后台主页面已齐

已完成：

- 登录
- 工作台统计页
- 用户管理
- 套餐管理
  - 新建
  - 编辑
  - 删除
  - 绑定 AI 配置
- 设备列表
- 拍摄记录
- AI 任务
- AI Provider 配置管理

当前特点：

- 已不再是骨架工程
- 核心后台页面已具备使用条件
- 仍可能存在 bundle 体积警告，但不阻塞当前交付

## 最近已完成的重要收口动作

以下能力是最近一轮工作中完成或明确收口的：

1. 正式图片上传链路
- 手机端先上传图片文件到 backend
- backend 落盘到本地静态目录
- 返回 `/uploads/...` URL
- 抓拍记录改为使用 backend 可访问地址

2. 真实 AI 调用框架
- backend 已具备真实 provider 调用层
- 支持 OpenAI 兼容格式
- `analyze-photo`
- `analyze-background`
- `batch-pick`

3. 套餐与 AI Provider 绑定
- 管理端可为套餐绑定 AI 配置
- backend 运行时按套餐优先选模型

4. 设备预览能力
- 设备端新增 `GET /api/device/preview.jpg`
- 设备联动页首屏可显示设备预览

5. 移动端产品化整改
- 重构设备联动页
- 补预览
- 支持 network 图片预览
- 收口首页与登录页文案
- 优化网络错误提示

## 已完成的回归与联调结果

### Flutter 工程回归

已通过：

- `flutter analyze`
- `flutter test`

### backend 主链回归

已通过：

- 登录
- 图片上传
- 会话创建
- 抓拍写库
- `analyze-photo`
- `analyze-background`
- `batch-pick`
- 历史抓拍回读
- 最佳图写回 `is_ai_selected`

### device_runtime 回归

已通过：

- `session/open`
- `preview.jpg`
- `apply-lock`
- `status`
- `session/close`

## 当前仍需注意的风险

1. 设备预览不是低延迟实时视频
- 当前是“最新帧图片预览”
- 能满足首版联动画面需求
- 但不是完整 RTSP/WebRTC 播放体验

2. 历史数据库里可能仍有旧演示数据
- 个别旧模板名或旧记录显示可能不够干净
- 这更偏旧数据问题，不是当前源码问题

3. device_runtime 日志观感仍不完美
- 本地文件流可能打印重连日志
- MediaPipe 可能 fallback 到 solutions API
- 当前功能可用，但日志不够干净

4. admin_web 仍有 bundle 体积警告
- 当前不阻塞功能或验收
- 但后续如继续扩展后台，可再做拆包优化

## 当前推荐的验收顺序

建议按这个顺序做人工验收：

1. 先验 backend 是否正常启动
2. 再验 admin_web
3. 再验 device_runtime
4. 最后验 mobile_client

推荐参考这些文档：

- [M8联调验收清单.md](</E:/BianChengWenJian/Camera Assistant/docs/M8联调验收清单.md:1>)
- [M8联调问题清单.md](</E:/BianChengWenJian/Camera Assistant/docs/M8联调问题清单.md:1>)
- [部署说明.md](</E:/BianChengWenJian/Camera Assistant/docs/部署说明.md:1>)
- [演示流程.md](</E:/BianChengWenJian/Camera Assistant/docs/演示流程.md:1>)

## 本地启动方式

### 1. 启动 backend

```powershell
cd "E:\BianChengWenJian\Camera Assistant"
.\.venv\Scripts\Activate.ps1
$env:DATABASE_URL="postgresql+psycopg://postgres:<your_password>@127.0.0.1:5432/camera_assistant"
python -m backend.main
```

### 2. 启动 device_runtime

```powershell
cd "E:\BianChengWenJian\Camera Assistant"
.\.venv\Scripts\Activate.ps1
uvicorn device_runtime.api.app:app --host 0.0.0.0 --port 8001
```

### 3. 启动 admin_web

```powershell
cd "E:\BianChengWenJian\Camera Assistant\admin_web"
npm run dev
```

### 4. 启动 mobile_client（Android 模拟器）

```powershell
cd "E:\BianChengWenJian\Camera Assistant\mobile_client"
flutter run --dart-define=API_BASE_URL=http://10.0.2.2:8000/api --dart-define=DEVICE_API_BASE_URL=http://10.0.2.2:8001
```

## 当前可用测试账号

### 管理员账号

- 手机号：`<admin_phone>`
- 密码：`<admin_password>`

### 手机端用户账号

- 手机号：`<user_phone>`
- 密码：`<user_password>`

## 建议给新对话窗口的使用方式

如果在新窗口继续使用 Codex，建议让它先看这几份文档：

1. [开发定稿.md](</E:/BianChengWenJian/Camera Assistant/docs/开发定稿.md:1>)
2. [Codex执行开发计划.md](</E:/BianChengWenJian/Camera Assistant/docs/Codex执行开发计划.md:1>)
3. [项目当前状态说明.md](</E:/BianChengWenJian/Camera Assistant/docs/项目当前状态说明.md:1>)
4. [开发进展记录.md](</E:/BianChengWenJian/Camera Assistant/docs/开发进展记录.md:1>)

这样能同时知道：

- 产品目标
- 开发计划
- 当前完成度
- 最近改动
