# draw.io graph LR 导入图

本文专门用于导入 draw.io / diagrams.net。

使用方式：

1. 打开 draw.io。
2. 选择 `Arrange` -> `Insert` -> `Advanced` -> `Mermaid`。
3. 每次只复制一张图的 `graph LR` 代码，不要复制 Markdown 的 ```mermaid 标记。
4. 导入后全选连接线，把线型设置为 `Orthogonal`，关闭 `Rounded`，即可得到横平竖直效果。
5. 方块和菱形如果大小不一致，导入后统一设置宽高即可。

统一出图要求：

- 从左到右排布。
- 方块表示动作或状态。
- 菱形表示判断。
- 箭头文字只写必要的“是 / 否 / 功能名”。
- 不使用回环箭头，循环逻辑用“继续扫描 / 继续检测 / 回到可拍摄”节点表达。

---

## 一、简洁时序图组

### 1. 总体时序图

```mermaid
graph LR
  A["用户进入<br/>系统"] --> B{"选择功能?"}

  B -->|"手机拍摄"| C["获取模板<br/>与历史"]
  C --> D["手机拍照"]
  D --> E["上传图片<br/>写历史"]
  E --> F{"后端 AI?"}
  F -->|"是"| G["调用 AI<br/>返回结果"]
  F -->|"否"| H["查看历史"]

  B -->|"设备联动"| I["打开设备<br/>会话"]
  I --> J["手机推流"]
  J --> K["预览回传<br/>状态同步"]
  K --> L{"设备功能?"}
  L -->|"AI"| M["设备端 AI<br/>返回建议"]
  L -->|"抓拍"| N["本地抓拍<br/>captures"]
```

### 2. 手机独立拍摄时序图

```mermaid
graph LR
  A["打开拍摄页"] --> B["获取模板"]
  B --> C["展示模板"]
  C --> D["手机拍照"]
  D --> E["创建 session"]
  E --> F["上传图片"]
  F --> G["创建 capture"]
  G --> H["返回历史记录"]
```

### 3. 后端 AI 分析时序图

```mermaid
graph LR
  A["发起 AI 分析"] --> B["请求后端接口"]
  B --> C["校验 capture"]
  C --> D["创建 ai_task"]
  D --> E["调用 AI Provider"]
  E --> F["返回评分<br/>总结建议"]
  F --> G["更新 ai_task"]
  G --> H["App 展示结果"]
```

### 4. 设备联动主链路时序图

```mermaid
graph LR
  A["打开设备联动"] --> B["打开设备会话"]
  B --> C["启动手机推流"]
  C --> D["发送 NV21 帧"]
  D --> E["树莓派检测<br/>构图 overlay"]
  E --> F{"控制云台?"}
  F -->|"是"| G["pan / tilt 控制"]
  F -->|"否"| H["保持当前姿态"]
  G --> I["回传 JPEG 预览"]
  H --> I
  I --> J["手机显示 HUD"]
```

### 5. 自动找角度时序图

```mermaid
graph LR
  A["启动自动<br/>找角度"] --> B["生成候选角度"]
  B --> C["云台扫描"]
  C --> D["保存候选帧"]
  D --> E{"扫描完成?"}
  E -->|"否"| F["继续扫描<br/>下一角度"]
  E -->|"是"| G["设备端 AI<br/>分析候选"]
  G --> H["生成最佳<br/>角度建议"]
  H --> I["返回 App"]
```

### 6. 背景锁机位时序图

```mermaid
graph LR
  A["启动背景<br/>锁机位"] --> B["生成候选机位"]
  B --> C["云台微调"]
  C --> D["保存候选画面"]
  D --> E{"扫描完成?"}
  E -->|"否"| F["继续微调<br/>下一机位"]
  E -->|"是"| G["分析人物<br/>背景关系"]
  G --> H["生成锁定建议"]
  H --> I["返回 App"]
```

### 7. 手势抓拍时序图

```mermaid
graph LR
  A["实时推流开启"] --> B["持续手势检测"]
  B --> C{"有效手势?"}
  C -->|"否"| D["继续检测<br/>下一帧"]
  C -->|"是"| E["启动 3 秒<br/>倒计时"]
  E --> F{"手势仍有效?"}
  F -->|"否"| G["取消倒计时<br/>继续检测"]
  F -->|"是"| H{"倒计时结束?"}
  H -->|"否"| I["继续倒计时"]
  H -->|"是"| J["执行抓拍<br/>保存本地"]
  J --> K["返回抓拍结果"]
```

### 8. 历史与缓存回退时序图

```mermaid
graph LR
  A["打开历史页"] --> B["请求后端历史"]
  B --> C{"在线成功?"}
  C -->|"是"| D["返回最新历史"]
  D --> E["更新本地缓存"]
  E --> F["展示最新历史"]
  C -->|"否"| G["读取本地缓存"]
  G --> H{"缓存存在?"}
  H -->|"是"| I["展示缓存历史"]
  H -->|"否"| J["显示错误提示"]
```

---

## 二、简洁流程图组

### 1. 总体流程图

```mermaid
graph LR
  A["用户进入系统"] --> B{"选择功能?"}

  B -->|"手机拍摄"| C["获取模板"]
  C --> D["拍照"]
  D --> E["创建 session"]
  E --> F["上传图片"]
  F --> G["写入历史"]
  G --> H{"发起后端 AI?"}
  H -->|"是"| I["调用 AI<br/>返回结果"]
  H -->|"否"| J["直接结束"]

  B -->|"设备联动"| K["打开设备会话"]
  K --> L["启动手机推流"]
  L --> M["检测 / 构图<br/>预览回传"]
  M --> N{"选择设备功能"}
  N -->|"控制"| O["手动控制<br/>模式切换"]
  N -->|"AI"| P["找角度<br/>锁机位"]
  N -->|"抓拍"| Q["本地抓拍"]
```

### 2. 手机独立拍摄流程图

```mermaid
graph LR
  A["进入拍摄页"] --> B["加载模板"]
  B --> C["打开手机相机"]
  C --> D["用户拍照"]
  D --> E["创建 session"]
  E --> F["上传图片"]
  F --> G["创建历史"]
  G --> H{"继续 AI 分析?"}
  H -->|"是"| I["调用后端 AI"]
  I --> J["展示 AI 结果"]
  H -->|"否"| K["结束"]
```

### 3. 后端 AI 分析流程图

```mermaid
graph LR
  A["发起 AI 分析"] --> B["校验 capture<br/>和 session"]
  B --> C{"校验通过?"}
  C -->|"否"| D["返回失败"]
  C -->|"是"| E["解析 Provider"]
  E --> F{"Provider 可用?"}
  F -->|"否"| G["创建 failed<br/>ai_task"]
  F -->|"是"| H["创建 pending<br/>ai_task"]
  H --> I["调用 AI Provider"]
  I --> J{"调用成功?"}
  J -->|"否"| K["更新 failed"]
  J -->|"是"| L["更新 succeeded"]
```

### 4. 设备联动主流程图

```mermaid
graph LR
  A["进入设备联动页"] --> B["加载模板<br/>连接配置"]
  B --> C["打开设备会话"]
  C --> D["启动手机推流"]
  D --> E["树莓派接收帧"]
  E --> F["检测 / 构图<br/>overlay"]
  F --> G{"需要控制云台?"}
  G -->|"是"| H["输出 pan / tilt"]
  G -->|"否"| I["保持当前姿态"]
  H --> J["回传预览"]
  I --> J
  J --> K["手机显示 HUD"]
```

### 5. 自动找角度流程图

```mermaid
graph LR
  A["启动自动<br/>找角度"] --> B["生成候选角度"]
  B --> C["云台逐个扫描"]
  C --> D["保存候选帧"]
  D --> E{"扫描完成?"}
  E -->|"否"| F["继续扫描<br/>下一角度"]
  E -->|"是"| G["设备端 AI<br/>分析候选图"]
  G --> H["选出最佳建议"]
  H --> I{"应用建议?"}
  I -->|"是"| J["云台转到<br/>建议位置"]
  I -->|"否"| K["仅展示建议"]
```

### 6. 背景锁机位流程图

```mermaid
graph LR
  A["启动背景<br/>锁机位"] --> B["生成候选机位"]
  B --> C["云台逐个微调"]
  C --> D["保存候选画面"]
  D --> E{"扫描完成?"}
  E -->|"否"| F["继续微调<br/>下一机位"]
  E -->|"是"| G["分析人物<br/>背景关系"]
  G --> H["生成锁定建议"]
  H --> I{"应用锁定?"}
  I -->|"是"| J["云台转到<br/>建议机位"]
  I -->|"否"| K["仅展示结果"]
```

### 7. 手势抓拍流程图

```mermaid
graph LR
  A["实时推流中"] --> B["持续手部检测"]
  B --> C{"有效手势?"}
  C -->|"否"| D["继续检测<br/>下一帧"]
  C -->|"是"| E["启动 3 秒<br/>倒计时"]
  E --> F{"手势仍有效?"}
  F -->|"否"| G["取消倒计时<br/>继续检测"]
  F -->|"是"| H{"倒计时结束?"}
  H -->|"否"| I["继续倒计时"]
  H -->|"是"| J["执行抓拍"]
  J --> K["保存 captures"]
  K --> L["返回结果"]
```

### 8. 模板流程图

```mermaid
graph LR
  A["进入模板功能"] --> B{"模板来源?"}
  B -->|"后端模板"| C["请求 mobile/templates"]
  B -->|"设备模板"| D["请求 device/templates"]
  C --> E["展示用户模板"]
  D --> F["展示树莓派模板"]
  E --> G{"新增模板?"}
  F --> H{"上传设备模板?"}
  G -->|"是"| I["上传照片<br/>生成模板"]
  G -->|"否"| J["保持列表"]
  H -->|"是"| K["上传树莓派<br/>生成本地模板"]
  H -->|"否"| L["保持列表"]
```

### 9. 历史与缓存流程图

```mermaid
graph LR
  A["打开历史页"] --> B["请求后端历史"]
  B --> C{"在线成功?"}
  C -->|"是"| D["展示最新历史"]
  D --> E["更新本地缓存"]
  C -->|"否"| F["读取本地缓存"]
  F --> G{"缓存存在?"}
  G -->|"是"| H["展示缓存历史"]
  G -->|"否"| I["显示错误提示"]
```

---

## 三、详细状态图中的全局总状态图

### 全局总状态图

```mermaid
graph LR
  A["系统入口"] --> B{"进入哪个<br/>状态域?"}

  B -->|"手机拍摄"| C["手机拍摄<br/>状态域"]
  C --> C1["相机准备"]
  C1 --> C2["可拍摄"]
  C2 --> C3["拍照中"]
  C3 --> C4["写历史中"]
  C4 --> C5{"写入成功?"}
  C5 -->|"是"| C6["回到可拍摄"]
  C5 -->|"否"| C7["写历史失败"]
  C2 --> C8["AI 分析中"]
  C8 --> C9{"AI 成功?"}
  C9 -->|"是"| C10["AI 结果可见"]
  C9 -->|"否"| C11["AI 失败"]

  B -->|"设备联动"| D["设备联动<br/>状态域"]
  D --> D1["会话未打开"]
  D1 --> D2["会话已打开"]
  D2 --> D3["推流中"]
  D3 --> D4["预览回传中"]
  D4 --> D5["设备功能运行中"]
  D5 --> D6["回到推流中"]
  D2 --> D7["会话关闭中"]
  D7 --> D8["回到未打开"]

  B -->|"历史缓存"| E["历史缓存<br/>状态域"]
  E --> E1["在线加载"]
  E1 --> E2{"在线成功?"}
  E2 -->|"是"| E3["最新数据可见"]
  E2 -->|"否"| E4["本地缓存回退"]
  E4 --> E5{"缓存存在?"}
  E5 -->|"是"| E6["缓存数据可见"]
  E5 -->|"否"| E7["加载失败"]
```
