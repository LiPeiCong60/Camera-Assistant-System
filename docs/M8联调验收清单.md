# M8 联调验收清单

## 说明

本清单用于收口前 8 批次的主链能力。状态分为：

- `通过`：已完成代码/接口/工程验收
- `部分通过`：主链已通，但仍建议做模拟器或真机手工点验
- `未通过`：当前仍存在阻塞问题

## 联调结果总表

| 项目 | 验收方式 | 结果 | 说明 |
| --- | --- | --- | --- |
| 手机端独立模式拍摄 | `flutter analyze` / `flutter test` / backend 联调脚本 | 部分通过 | 工程通过，拍照上传链路通过，仍建议做模拟器/真机手工点验 |
| 手机端模板显示 | backend `templates` 接口联调脚本 | 通过 | 模板创建成功，模板列表返回成功 |
| 手机端上传分析 | backend 联调脚本 | 通过 | 文件上传 200，创建会话 200，`analyze-photo` 成功 |
| 手机联动树莓派 | `device_runtime` TestClient 联调 | 通过 | `health`、`session/open`、`status`、`session/close` 均成功 |
| 树莓派手动控制 | `device_runtime` TestClient 联调 | 通过 | `manual-move`、`mode`、`follow-mode` 成功 |
| 树莓派追踪基础状态 | `device_runtime` TestClient 联调 | 部分通过 | 模式切换与状态回显通过，真实画面追踪仍建议手工确认 |
| 模板引导自动调机位 | backend/mobile/device 组合联调 | 部分通过 | 模板选择、会话模式 `SMART_COMPOSE` 已打通；真实视觉效果仍建议手工确认 |
| AI 锁机位执行 | backend + device 联调 | 通过 | `analyze-background` 成功，`apply-lock` 后 `ai_lock_status.enabled=true` |
| 连拍选优 | backend 联调脚本 | 通过 | `batch-pick` 成功，最佳图写回 `is_ai_selected=true` |
| 管理端用户与套餐 | `npm run build` + 真实后端接口 | 通过 | 用户列表、套餐增改删、AI 配置绑定均已验证 |
| 管理端设备列表 | `npm run build` + `GET /api/admin/devices` | 通过 | 设备列表页已接通真实接口 |
| 管理端拍摄记录与 AI 任务 | `npm run build` + 真实后端接口 | 通过 | 列表页和 AI 任务详情接口均已接通 |
| 管理端 AI 配置 | 真实后端接口 + 页面逻辑 | 通过 | 多厂商、多模型、多密钥配置已接通 |

## 关键验收记录

### 1. backend 主链联调结果

说明：
- 本轮 backend 主链联调采用 `TestClient + provider 方法打桩` 的方式验证。
- 也就是说：上传、写库、任务创建、任务状态流转、结果写回、历史查询都是真实链路。
- 但外部厂商网络请求本身未在这轮自动化联调里直接出网调用。

- 模板创建：`200`
- 模板列表：`200`
- 图片上传：`200`
- 拍摄会话创建：`200`
- 会话模式：`SMART_COMPOSE`
- 抓拍写库：`200`
- `analyze-photo` 任务状态：`succeeded`
- `analyze-photo` provider 模式：`real_provider`
- `analyze-background` 任务状态：`succeeded`
- `analyze-background` provider 模式：`real_provider`
- `batch-pick` 任务状态：`succeeded`
- `batch-pick` 最佳图 ID：联调脚本已成功返回
- 历史会话：`200`
- 历史抓拍：`200`
- 最佳图 `is_ai_selected=true`：已验证
- 历史抓拍 `file_url`：已为 backend `/uploads/...` URL，不再是 `file://`

### 2. device_runtime 主链联调结果

- `GET /api/device/health`：`200`
- `POST /api/device/session/open`：`200`
- `GET /api/device/status`：`device_status=online`
- `POST /api/device/control/manual-move`：`200`
- `POST /api/device/control/mode`：`200`
- `POST /api/device/control/follow-mode`：`200`
- `POST /api/device/templates/import`：`200`
- `POST /api/device/templates/select`：`200`
- `POST /api/device/ai/apply-lock`：`200`
- `GET /api/device/status`：`ai_lock_status.enabled=true`
- `POST /api/device/session/close`：`200`

### 3. 前端工程验收结果

#### mobile_client

- `flutter analyze`：通过
- `flutter test`：通过

#### admin_web

- `npm run build`：通过

## 建议的手工补充验收

1. 在 Android 模拟器中手工验证：
   - 登录
   - 拍照页进入
   - 拍照上传
   - 历史页查看
   - 设备联动页状态刷新
2. 在管理端浏览器中手工验证：
   - 用户页
   - 套餐页
   - 设备列表
   - 拍摄记录
   - AI 任务详情
   - AI Provider 配置
3. 使用真实可用的厂商 Key，在管理端选定目标 Provider 后，手工跑一次：
   - `analyze-photo`
   - `analyze-background`
   - `batch-pick`
   以确认外部网络调用链在当前环境下也可用
